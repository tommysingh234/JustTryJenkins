pipeline {
    agent any

    tools {
        maven 'Maven3'
    }

    stages {

        stage('Build & Run Tests') {
            steps {
                sh 'mvn clean test'
            }
        }
    }

    post {
        always {
            junit 'target/surefire-reports/*.xml'
        }
    }
}
